# modeling_v8 결과 보고서 06 — M5a (모델 후보 벤치마크)

> **프로젝트**: SK하이닉스 반도체 결함 예측 (FDC Trace → C65 회귀)
> **문서 성격**: **스냅샷 보고서**(고정) — 이 파일은 *M5a*(모델 후보 벤치) 결과만 기록하며 이후 마일스톤이 나와도 **수정하지 않는다**. 선행: `modeling_v8_REPORT_05_M4.md`. 누적 로그·인덱스는 `../modeling_v8_README.md`.
> **범위**: M5a — 후보 7종 × 3트랙(F-C10·F-T15·F-P3) 동일 fold 벤치 + OOF 오차상관 + 상위2 선정. PLAN **v1.6** §8.3·§8.5.
> **작성 기준(2026-07-10)**: `modeling_v8.ipynb` Cell 13~14 실행 결과(로컬 확정). 산출물: `outputs/{model_bench.csv, model_bench_corr.csv, results_M5a.json}`.

---

## 0. 무엇을 보는가

M4가 코어 10을 G2로 **동결**했다. 하지만 그 우위는 지금까지 **LightGBM 한 학습기** 위에서만 확인됐다. M5a는 피처를 그대로 두고 **"이 피처에서 LightGBM이 정말 최선인가"**를 후보 7종으로 데이터에 묻는다. 동시에 §8.5 관찰 트랙 2종(F-T15·F-P3)을 나란히 측정해 "코어 10이 모델을 바꿔도 이기는가"를 본다.

**프로토콜**: 입력·fold 통일(`KFold(5, shuffle, 42)` — M0~M4와 동일), 동일 OOF→WF-RMSE. 후보 5·6(ExtraTrees·선형)의 대치·표준화·스플라인은 **fold 내부에서 fit**(누수 방지). 선정은 **F-C10 열 CV 최소로만**, valid는 선정 후 확인용(R10). 트랙은 F-C10 상위 3모델에서만 평가(D8).

**하니스 정합 체크 — 전부 통과**: ① LGBM×F-C10 CV **40.347 = cv_m0b(40.35)** 정확 재현(동일 fold·파라미터). ② LGBM×F-T15 CV **43.937 ≈ 동결표 43.94**, valid/test **41.674/43.253 = 동결표 41.67/43.25**. → 벤치 하니스·방법론이 M2와 동일함을 수치로 확증.

---

## 1. F-C10 벤치 — 7종 전체 (동결 코어 10)

| 순위 | 모델 | CV(wafer,OOF) | fold std | 학습시간 | ΔvsLGBM | 비고 |
|:---:|------|:---:|:---:|:---:|:---:|------|
| **1** | **ExtraTrees** | **38.001** | 0.503 | 8.6s | **−2.35** | 배깅·NaN 중앙값 대치 |
| 2 | LGBM (기준) | 40.347 | 0.944 | 84.3s | — | = cv_m0b ✅ |
| 3 | SegLGBM (레짐 세그) | 40.840 | 0.989 | 139.5s | +0.49 | pre/post 2모델 |
| 4 | XGBoost | 41.435 | 1.019 | 18.5s | +1.09 | hist, NaN 네이티브 |
| 5 | CatBoost | 41.665 | 1.273 | 59.9s | +1.32 | ordered boosting |
| 6 | HistGB | 50.970 | 0.492 | 8.1s | +10.62 | 기본 파라미터 열세 |
| 7 | Ridge+spline | 67.141 | 0.785 | 0.5s | +26.79 | 선형은 부적합 |

**읽기.** ① **부스팅 3종(LGBM·XGB·CatBoost)은 40.3~41.7로 근접**(±1.3pt) — PLAN 예상("부스팅 3종 근접")대로. LGBM이 그중 최선이라 v1~v7의 LGBM 선택은 부스팅 안에서는 타당했다. ② **레짐 세그먼트(SegLGBM)는 단일 LGBM에 +0.49 열세** — 레짐별 분산 차(40.1 vs 159.4)를 명시적으로 라우팅해도 이득이 없다(pre 3,692 WF 소표본에서 각자 과적합). ③ **선형(Ridge+spline)은 67.1로 완전 부적합** — 타깃이 "레짐 레벨+단조 감쇠"만은 아니며 센서 상호작용이 트리를 요구함을 역으로 확인. ④ **ExtraTrees가 38.00으로 단독 선두**(−2.35pt) — 아래 2·4절에서 신뢰성을 따로 검증한다.

---

## 2. 관찰 트랙 (§8.5) — 상위 3모델 × F-T15·F-P3

| 모델 | F-C10 | F-P3 (13f) | F-T15 (15f) | 트랙 최선 여부 |
|------|:---:|:---:|:---:|------|
| ExtraTrees | **38.001** | 38.807 | 40.906 | F-C10 < F-P3 < F-T15 |
| LGBM | **40.347** | 41.052 | 43.937 | F-C10 < F-P3 < F-T15 |
| SegLGBM | **40.840** | 41.492 | 44.274 | F-C10 < F-P3 < F-T15 |

**모델을 무엇으로 바꾸든 순서가 동일하다 — 코어 10(F-C10) < F-P3 < F-T15.** 즉 F-P3의 추가 3종(C12×2·C4×1, 레짐 프록시)과 F-T15의 자동선택 15종(집단 필수 센서 C59/C60 탈락)은 **어느 학습기에서도 코어 10을 못 넘는다**. G2 피처 동결이 LGBM 특이현상이 아님을 3모델로 재확인 → **§8.5 관찰 트랙, M5a 구간 전부 열세**(은퇴 근거 1건 누적).

### 트랙 valid/test 최초기록 (LGBM 레퍼런스, 마일스톤 1회)

| 트랙 | CV | valid | test | 상태 |
|------|:---:|:---:|:---:|------|
| F-C10 (챔피언) | 40.347 | 38.399 | 38.912 | = M0b |
| F-T15 | 43.937 | 41.674 | 43.253 | 동결표 재현 ✅ |
| **F-P3** | 41.052 | **39.162** | **39.525** | **M5a 최초 조회** |

F-P3의 valid/test가 처음 기록됐다(§8.5 예고분). **CV·valid·test 세 지표 모두 F-C10에 열세**(+0.71/+0.76/+0.61) — C12 레짐 프록시가 랜덤 CV에서 중복·노이즈라는 M2 판정을 valid/test로도 확증. (H-T1 "COLD 상황 백업 피처셋" 가설은 M8-S2 전용이라 여기선 미검정 — 트랙은 계속 추적.)

---

## 3. OOF 오차 상관 — M6 앙상블 다양성 (보고용)

F-C10 7모델 잔차 상관(낮을수록 앙상블 원료):

| | LGBM | XGB | CatB | HistGB | **ExtraTrees** | Ridge |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| 부스팅 3종끼리 | — | 0.93 | 0.93 | — | — | — |
| **ExtraTrees ×** | 0.79 | 0.78 | 0.76 | 0.61 | — | **0.46** |
| Ridge × | 0.63 | 0.64 | 0.68 | 0.87 | 0.46 | — |

부스팅 3종은 서로 0.91~0.95로 사실상 한 덩어리(앙상블 이득 작음). **ExtraTrees가 부스팅과 0.76~0.79로 가장 탈상관**, Ridge와는 0.46. → M6를 연다면 **ExtraTrees + 부스팅 1종**(다른 편향 구조) 또는 **ExtraTrees + Ridge**(가장 탈상관, 단 Ridge 단독 성능 열세)가 후보. `outputs/model_bench_corr.csv`에 13×13 전체 보관(트랙 조합 포함).

---

## 4. 선정 — ExtraTrees **잠정** 채택, 검증 대기 ⚠️

기계적 선정 규칙(F-C10 CV 최소)은 **ExtraTrees(38.001)**를 채택하고 상위 2개 **[ExtraTrees, LGBM]**를 M5b로 올린다. 그러나 이 우위는 **아직 확정이 아니다** — 아래를 반드시 통과해야 한다.

**왜 잠정인가 (누수 경계).** 벤치 CV는 **랜덤 wafer-KFold**다. 이 캠페인은 **Lot 1,217개가 스팬 0.357h**(같은 Lot의 wafer들이 센서까지 거의 동일)라, 랜덤 분할은 **Lot 동료(lot-mate)를 train/test에 쪼개 넣어** 낙관 편향을 만든다(v5 Lot-CV 갭 +7.8의 원인). **ExtraTrees는 500그루 무가지치기 배깅**으로 이 lot-mate 유사도를 **가장 잘 암기·악용**할 수 있는 구조인 반면, LGBM은 강한 정칙화(`lambda_l1=5.04`, `min_split_gain=0.257`)로 이를 억제한다. 즉 −2.35pt가 **일반화 이득인지 lot-mate 암기인지 아직 구분되지 않았다.**

**단, 노이즈는 아니다.** ExtraTrees는 early stopping을 안 써 **fold-test를 훔쳐보지 않는데도**(부스팅 3종은 fold-val early stop으로 미세 낙관 편향 有) 이겼고, fold std도 0.50으로 낮다. 트랙 순서도 일관. → 랜덤 CV 상의 **실재하는 현상**이며, 남은 것은 그것이 **어디서 오는가**의 판정이다.

**결정적 tell(가용).** LGBM은 valid(38.40)가 CV(40.35)보다 **−1.95pt 좋다** — 정칙화 모델에겐 랜덤 CV가 오히려 비관적. **ExtraTrees가 lot-mate를 암기 중이라면 valid는 CV(38.00)보다 나빠져야** 한다. 따라서 **ExtraTrees valid/test 1회 조회**가 즉시 판정을 내린다(선정 후 확인용 = 계획 §8.3-3 규율 부합). → 확인 셀 `[Cell 15]` 별도 전달, **M5b 진입 전 게이트**로 사용.

- **valid ≈ 38 → 실재 챔피언 도전자.** M5b에서 ExtraTrees·LGBM 둘 다 튜닝 진행.
- **valid ≥ 41 → 랜덤 CV 과적합(lot-mate).** 챔피언은 LGBM 유지, ExtraTrees는 M6 앙상블 다양성 원료로만 강등.
- 어느 쪽이든 **최종 판정은 M7의 Lot-CV(GroupKFold C20)·time-split**이 확정한다(원래 정직성 게이트).

---

## 5. 판정 & 다음

- **피처(G2)는 그대로.** 코어 10이 3모델 전부에서 F-P3·F-T15를 이김 → 동결 유지, 관찰 트랙 M5a 전 구간 열세(은퇴 근거 누적).
- **모델 선정은 잠정.** ExtraTrees가 랜덤 CV −2.35pt 선두이나 **lot-mate 누수 가능성으로 검증 대기**. 부스팅 3종은 40.3~41.7 근접(LGBM 최선). 선형·세그먼트는 부적합.
- **다음 (즉시)**: `[Cell 15]` 확인 셀 — ExtraTrees F-C10 **valid/test 조회**로 일반화/암기 판정. 결과에 따라 M5b 튜닝 대상 확정. 그 후 **M7**(Lot-CV·time-split)이 3트랙·최종모델로 최종 확정.
- 산출물: `outputs/model_bench.csv`(벤치 표) · `outputs/model_bench_corr.csv`(13×13 오차상관) · `outputs/results_M5a.json`(bench+트랙 valid/test+선정, `feature_set` 필드 포함).

> **한 줄 요약**: 코어 10은 모델을 바꿔도 최선(G2 견고). ExtraTrees가 랜덤 CV에서 −2.35pt 선두이나 lot-mate 누수 의심 → valid/test 확인(Cell 15) 후 M5b. LGBM은 안전한 현 챔피언.
